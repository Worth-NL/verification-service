{
  "name": "verification-service",
  "version": "0.2.3",
  "description": "This repository is the beginning of a simple email and telephone number verification service to be used by public institutions in the Netherlands. We will be aiming to deliver a proof of concept of this codebase in september 2025. After that we will be making some important decisions on how to morph/move/discard this code in favor of something else.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "start": "next start",
    "start:prod": "node dist/dbsetup.js && next start",
    "start:cron": "node dist/cron-task.js",
    "build": "next build && tsc -p tsconfig.build.json",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "next": "^15.5.7",
    "node-cron": "^4.2.1",
    "notifications-node-client": "^8.2.1",
    "react": "^19.1.1",
    "redoc": "^2.5.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "prisma": "^6.14.0",
    "typescript": "^5"
  }
}
